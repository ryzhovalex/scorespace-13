spawn_radius = 180
spawn_strength = 0 // how much force (in speed) will be added to spawned ball 
spawn_delay = 5 * room_speed // how much time in seconds spawner prepare for the spawn

function spawn(ball, ball_speed) {
    alarm[0] = spawn_delay
}
